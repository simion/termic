// Settings UI strings. One block per settings page, plus shared chips
// (shared) and the rail itself (rail). `general.language` predates the
// extraction pass and is wired into GeneralSection; keep it verbatim.
export default {
  shared: {
    saved: "Saved",
    saveFailed: "Save failed",
    experimental: "Experimental",
    stop: "Stop",
    restore: "Restore",
  },

  rail: {
    close: "Close settings",
    closeArmed: "Esc again to close",
    general: "General",
    appearance: "Appearance",
    agents: "Agents & Terminals",
    tasks: "Tasks",
    notifications: "Notifications",
    prompts: "Prompts",
    shortcuts: "Shortcuts",
    profiles: "Profiles",
    sandbox: "Sandbox",
    docker: "Docker Sandbox",
    cli: "CLI & MCP",
    badgeExp: "Exp",
    projects: "Projects",
    noProjects: "No projects yet.",
    pickProject: "Pick a project on the left to edit its settings.",
    viewChangelog: "View the changelog",
    checkUpdates: "Check for updates",
    updateAvailable: "Update available",
    updateCheckFailed: "Update check failed",
    upToDate: "You're up to date",
  },

  general: {
    language: {
      title: "Language",
      hint: "Interface language. System default follows the macOS language: Chinese shows Simplified Chinese, anything else shows English.",
      system: "System default",
      en: "English",
      zhCN: "简体中文",
    },
    reposDir: {
      title: "Repos directory",
      hint: "Where Termic scans for unadded git repos when you click \"Add project\".",
    },
    forge: {
      title: "Pull request integrations",
      reprobe: "Re-detect the CLIs",
      hint: "PR/MR status, creating a PR, merge detection, and starting a task from an issue all run through the official CLIs. Termic never stores tokens: sign in once with each CLI and it all works. Self-hosted GitHub Enterprise and GitLab work too, as long as the CLI is signed in to that host.",
      notInstalled: "Not installed.",
      notSignedIn: "Installed, not signed in.",
      signedIn: "Signed in",
      signedInAs: "Signed in as <1>{{account}}</1>",
      probing: "Probing CLIs…",
    },
    hidden: {
      title: "Hidden files (personal)",
      hint: "Patterns hidden from the \"All files\" tree across every project on this machine. Pick a preset or add your own. For team-shared, per-repo excludes, use a project's <1>.termic.yaml</1> (Settings → Projects).",
      save: "Save hidden files",
    },
    browser: {
      title: "Open links in",
      hint1: "Termic opens a link in three places: the globe button that appears while a run is going, the Open button on the run toolbar, and any URL you {{mod}}-click in a terminal. This setting picks the browser for all three. Everything else (the Termic website, an issue link) keeps using your system default.",
      hint2: "It is a command, not just an app, so you can pick a specific browser profile. Leave it empty to use your default browser, exactly as before. One project can override this in Settings → Projects.",
      save: "Save browser",
    },
    closeAction: {
      label: "When you close the window",
      hint: "Closing used to quit Termic and stop every running agent. Keeping them in the menu bar leaves them working, and Quit (⌘Q, or the menu-bar item) becomes the only thing that stops them.",
      ask: "Ask me each time",
      menubar: "Keep agents running in the menu bar",
      quit: "Quit Termic and stop agents",
    },
    tray: {
      label: "Show Termic in the menu bar",
      hint: "A small icon that's always there while Termic is running: a badge and dropdown for tasks that need your input or just finished, and Show/Quit.",
      hintMac: " Turning it off also means closing to the menu bar (above) falls back to the dock icon as your way back in.",
    },
    touchId: {
      label: "Offer Touch ID for sudo",
      hint: "When a terminal asks for your sudo password, offer to turn on Touch ID for sudo. Accepting runs a short script, in a tab where you can read it, that adds pam_tid.so to /etc/pam.d/sudo_local.",
    },
    remoteImages: {
      label: "Load remote images in markdown preview",
      hint: "Off by default: images hosted on external sites are blocked in the markdown preview, so opening an untrusted file (a dependency's README, a fetched page) can't silently fire a network request. A per-document button in the preview can still load them for just that file.",
    },
  },

  tasks: {
    path: {
      title: "Default tasks path",
      hint: "Where new task worktrees are created. A full path (one starting with <1>/</1> or <3>~</3>) keeps every project's tasks under one root, in a folder named after the project. A relative path puts each project's tasks inside that project's own directory instead. Existing tasks never move; this applies to the next one you create.",
      required: "A tasks path is required.",
      conflictOne: "This lands inside the repo itself for {{names}}. New tasks there would be created on top of your working tree. Pick a directory outside the repo, or a subdirectory of it.",
      conflictMany: "This lands inside the repo itself for {{count}} projects: {{names}}. New tasks there would be created on top of your working tree. Pick a directory outside the repo, or a subdirectory of it.",
      preview: "New tasks go to",
      save: "Save tasks path",
    },
    branchPrefix: {
      title: "Branch prefix",
      hint: "Prepended to auto-generated branch names for new tasks (<1>{{preview}}</1>). Leave empty for no prefix. You can still edit the branch per task.",
    },
    branchName: {
      label: "Use the branch name as the task name",
      hint: "Label each worktree task by its branch instead of the title typed when it was created, in the sidebar, the breadcrumb and everywhere else a task is named. Tasks running in a project's main checkout, and tasks in a plain folder, keep their title. The typed name stays in the row tooltip, and renaming a task still edits it.",
    },
    fetch: {
      label: "Fetch base before creating a task",
      hint: "Refresh the base branch from its remote (a quick, single-ref git fetch) right before a new task's branch is cut, so it starts from the latest commit instead of a stale local copy. Best-effort: if the remote is offline or unreachable, the task still creates from your local ref. Turn off on flaky networks.",
    },
    ports: {
      title: "Task port range",
      hint: "The window <1>$TERMIC_PORT</1> and every extra named port are allocated from. Each task takes a consecutive block, so the range needs room for a few ports per task. Existing tasks keep the ports they were given; this applies to the next task you create.",
      to: "to",
      room: "Room for about {{count}} tasks.",
      lowWarn: " Ports this low are where dev servers usually live. Pick a range nothing else on this machine uses, or a server started on a port that is already taken will fail to bind.",
      save: "Save port range",
    },
    symlinks: {
      title: "Worktree config symlinks",
      hint: "Repo-root files and folders symlinked into each new worktree task, one per line, so agents keep project config (subagents, skills, commands, MCP servers) that is gitignored out of a plain checkout. Only entries that exist in the repo are linked. Clear the list to turn this off.",
      label: "Paths to symlink",
      save: "Save symlink paths",
    },
    queue: {
      title: "Queue send interval",
      hint: "Minimum delay between consecutive queued messages sent to an agent (the \"ralph loop\"). Even if the agent finishes faster, or a false \"done\" fires, the next message waits this long. Set to 0 to disable. \"Send now\" ignores this and sends immediately.",
      seconds: "seconds",
    },
    confirmClose: {
      label: "Confirm before closing an agent tab",
      hint: "Ask before closing a non-shell terminal or agent tab. Turning this off (or unchecking it once from the close dialog) closes tabs immediately; a toast then points back to the '+' menu's Resume section to bring one back.",
    },
    confirmArchive: {
      label: "Confirm before archiving a task",
      hint: "Ask before archiving a task. With this off (or after unticking \"Show this every time\" in the dialog), archiving happens straight away and a toast points at History.",
    },
    confirmRestart: {
      label: "Confirm before restarting an agent to switch account",
      hint: "Switching a task to another credential set needs the agent to restart, which resumes the conversation. With this off the restart happens straight away.",
    },
    deleteBranch: {
      label: "Delete the branch when archiving",
      hintOn: "Start the archive dialog's \"Delete the git branch\" box ticked. You can still untick it for any single archive. A project's main checkout is never affected.",
      hintOff: "Archiving also deletes the task's branch. A project's main checkout is never affected.",
    },
  },

  notifications: {
    desktop: {
      label: "Desktop notifications",
      hint: "Notify when an inactive agent finishes or rings the bell. Clicking back in jumps to that tab.",
    },
    sound: {
      label: "Completion sound",
      hint: "Pick which sound plays inside desktop notifications when an inactive agent finishes a turn. Default: Funk.",
      preview: "Preview",
      previewTip: "Play a preview of the selected completion sound",
      lockedNote: "Turn on Desktop notifications above to enable completion sounds.",
      taskFallback: "task",
      titleFallback: "project · task",
      body: "agent finished",
    },
    marksTitle: "Agent status marks",
    markWorking: {
      label: "Working",
      hint: "The agent is working right now. Covers every mid-turn mark, including the two below.",
    },
    markDelegated: "Waiting on subagents or scripts it started. Nothing is being computed, and this can last hours. Follows Working.",
    markPartial: {
      label: "Partially done",
      hint: "Some of that work reported back while the rest still runs. Off leaves the mark above until everything is in. Either way the turn is not called done until it is.",
    },
    markDone: {
      label: "Work done",
      hint: "The agent finished its turn. This is the mark a desktop notification goes with.",
    },
    markAttention: {
      label: "Needs attention",
      hint: "The agent is blocked on you: a permission prompt, a question, or anything else it cannot get past on its own.",
    },
    hooksNote: "These marks read Termic's idea of what an agent is doing. To have the agent report that itself instead, see <1>Agent hooks</1> under Agents & Terminals.",
  },

  sandbox: {
    defaultKind: {
      title: "Sandbox new tasks by default",
      hint: "The New task dialog's sandbox picker starts here for every project, unless the project sets its own default or you've already picked something for a previous task (that habit wins). Individual projects can still override (Settings → Repositories).",
      dockerUnavailable: "Enable Docker sandbox and build the image in Settings → Docker Sandbox first.",
    },
    bypass: {
      label: "Bypass permissions in sandboxed tasks",
      hint: "When on, agents in a sandboxed task skip their own permission prompts. The macOS seatbelt is the real boundary. Turn off to make sandboxed agents still ask. Applies to newly spawned terminals.",
    },
    yoloDefault: {
      label: "Start new tasks in YOLO",
      hint: "For a machine that is already the sandbox, like a dedicated Mac mini or a VM. New tasks start with the agent's own permission prompts skipped, and for codex and muse that also turns off their built-in sandbox. The New task dialog shows it before you create, and a project can override it (Settings → Repositories). Agents creating tasks through the CLI still need <1>--yolo</1>.",
    },
    global: {
      title: "Global sandbox defaults",
      hint: "One per line. Wildcards (<1>*.example.com</1>) for hosts; <3>$HOME</3> + <5>~</5> expand for paths. Merged with each project's own lists when a task is created.",
      allowedPaths: "Allowed paths",
      allowedHosts: "Allowed hosts",
      save: "Save defaults",
    },
  },

  cli: {
    title: "Termic CLI",
    enable: {
      label: "Enable CLI",
      hint: "Let the {{name}} command drive this app from any shell: create tasks and stream their setup, wait for an agent to go quiet, list and check tasks, archive them, and add or remove projects. On by default, and access needs a token only this Mac's user can read. Agents in an enforced sandbox never get access. Turning this off refuses every command immediately (the command stays installed).",
    },
    installedAt: "<1>{{name}}</1> is installed at <3>{{path}}</3>.",
    runList: "Run <1>{{command}}</1> from any shell.",
    notOnPath: "That location is not on your PATH yet, so the command will not be found until you add it.",
    autoInstall: "Enabling installs <1>{{name}}</1> into <3>~/.local/bin</3> automatically.",
    installSystemWideOptional: "Install system-wide instead (optional, uses /usr/local/bin)",
    installing: "Installing…",
    addToPath: "Add to PATH",
    addToPathHint: "adds <1>~/.local/bin</1> to your shell startup file (no password)",
    installSystemWide: "Install system-wide",
    systemWideHint: "symlinks into <1>/usr/local/bin</1> (asks for your password)",
    working: "Working…",
    started: {
      title: "Getting started",
      hint: "Run these from inside a registered repo. <1>{{help}}</1> lists the full surface.",
    },
    agents: {
      title: "Agents can drive it too",
      hint: "A task's terminals are handed the command's path and the task's own id, so an agent working in one can create more tasks, wait for them to finish, and read what they produced. That is how one agent farms work out to several in parallel. Agents in an enforced sandbox are refused, so this applies to unsandboxed tasks only.",
      foot: "No setup on the agent's side: it finds everything from <1>$TERMIC_CLI</1> and <3>{{helpJson}}</3>.",
    },
  },

  mcp: {
    title: "MCP endpoint",
    enable: {
      label: "Enable MCP endpoint",
      hint: "Serve this app's tasks and projects as MCP tools on a local-only address, for clients that cannot use the CLI. Off means nothing is listening. Access needs a token only your user account can read.",
    },
    connect: "Connect a client",
    reading: "Reading the endpoint state...",
    listening: "Listening here. Clients authenticate with the token in <1>{{path}}</1>.",
    copyToken: "Copy token to clipboard",
    tokenNote: "For clients that take only a pasted value. The token changes on every restart, so prefer the setups below, which read the file.",
    addToCodex: "Add to Codex",
    codexHint: "Writes this to <1>~/.codex/config.toml</1>, backing up the current file. It also turns on the protocol revision, which is a codex-wide setting.",
    addToClaude: "Add to Claude Code",
    claudeHint: "Needs no extra setting, on 2.1.238 or newer. Registered through claude's own command, so a running session cannot clobber the config.",
    bindFailed: "The endpoint could not bind. Check the log, or toggle the setting off and on.",
    noToken: "The endpoint is not running, so there is no token",
    adding: "Adding…",
    copyUrlLabel: "MCP URL",
    copyCodexLabel: "codex config",
    copyClaudeLabel: "claude command",
    copyTokenLabel: "MCP token",
  },

  appearance: {
    reset: "Reset to defaults",
    resetTip: "Restore all Appearance settings (fonts, sizes, zoom, spacing, and terminal options) to their defaults.",
    tabTerminal: "Terminal",
    tabEditor: "Editor",
    tabInterface: "Interface",
    termFont: {
      label: "Terminal font",
      hint: "Font for all xterm terminals (main + scratch shell).",
    },
    termFontSize: {
      label: "Terminal font size",
      hint: "{{size}}px",
    },
    letterSpacing: {
      label: "Terminal letter spacing",
      hint: "{{spacing}}px added per cell. xterm packs glyphs snug; bump 1–2px to match iTerm / Terminal.app spacing.",
      compact: "Compact",
      default: "Default",
      roomy: "Roomy",
      wide: "Wide",
    },
    scrollback: {
      label: "Terminal scrollback",
      hint: "{{count}} lines. Agent terminals keep this many lines; the scratch shell keeps half.",
    },
    meta: {
      label: "Use {{alt}} as Meta key",
      hint: "Send {{alt}}+key as an ESC-prefixed sequence so terminal editors (vim, emacs, nano) see it as Meta/Alt. When off, {{alt}} types accented characters as usual.",
    },
    renderer: {
      label: "Terminal renderer",
      hint: "GPU (WebGL) is the default and the right choice for almost everyone. Switch only if you hit glitches: text drawn wrong, flicker or leftover artifacts, or typing that lags on a setup running WebGL through a software rasterizer. Canvas and DOM both cost more than twice the CPU of WebGL under heavy output, so there is no performance reason to move off it. Applies to terminals opened after the change.",
    },
    preview: {
      show: "Show live preview",
      hint: "Runs a real shell here so font, size and spacing render exactly as they will in a task.",
    },
    editorFont: {
      label: "Editor font",
      hint: "Font for the code editor and diff viewer.",
    },
    themeDark: {
      label: "Editor theme (dark)",
      hint: "Syntax color scheme for the code editor and diff viewer, when termic's own theme is dark.",
    },
    themeLight: {
      label: "Editor theme (light)",
      hint: "Syntax color scheme for the code editor and diff viewer, when termic's own theme is light.",
    },
    editorFontSize: {
      label: "Editor font size",
      hint: "{{size}}px",
    },
    ligatures: {
      label: "Code ligatures",
      hint: "Render font ligatures like `=>`, `!==`, `>=` as combined glyphs in the editor.",
    },
    wordWrap: {
      label: "Word wrap",
      hint: "Wrap long lines to the width of the editor instead of scrolling sideways. Also in the command palette.",
    },
    blame: {
      label: "Inline git blame",
      hint: "Show who last changed the line the cursor is on, after the code. Click it to open that commit's diff.",
    },
    intel: {
      desc: "Go to definition, find usages, an outline of the file, and types on hover, using the same tools a full IDE uses.",
      descErrors: " Type errors are underlined too.",
      descTail: " Nothing runs until you switch it on for a project, from the compass button above the editor.",
      offerLabel: "Offer it in the editor",
      offerHint: "Turning this off hides the button everywhere and runs nothing at all.",
      typeCheckLabel: "Type checking",
      typeCheckHint: "Underlines type errors, using your project's own checker config. Expect false alarms on framework code (missing stubs, a library's own annotations): each underline names the rule, so you can silence it in that config. Navigation works either way.",
      serversLabel: "Language servers",
      serversSub: "what can run, and what this machine already has",
      serversHint: "One per language, started only for a project you switch this on for. Where a language has more than one, pick the one you want; Automatic uses the order below. A copy inside the project (<1>node_modules</1>, a virtualenv) always wins over these.",
    },
    windowHeading: "Window",
    uiZoom: {
      label: "UI zoom",
      hint: "{{scale}}% of native. Scales the whole app (sidebar, tabs, files and git panels, terminals) like browser zoom.\nShortcuts: {{cmd}} +, {{cmd}} -, {{cmd}} 0.",
    },
    sidebarHeading: "Sidebar",
    expand: {
      label: "Task expand behavior",
      hint: "How a task's agent list reveals itself in the sidebar.",
      chevron: "Chevron only",
      chevronHint: "Row click only activates. Use the chevron to expand.",
      click: "Click name",
      clickHint: "Click the active row's name to toggle; auto-expand at 2+ agents.",
      always: "Auto open",
      alwaysHint: "Start expanded. The chevron still collapses, and that sticks.",
    },
    hoverReveal: {
      label: "Hover to reveal the collapsed sidebar",
      hint: "Slides the full sidebar out over the icon rail on hover.",
    },
    panesHeading: "Panes",
    dim: {
      label: "Dim inactive split panes",
      hint: "Overlay a dark mask over split panes that do not have keyboard focus.",
      amountLabel: "Dimming amount",
      amountHint: "{{amount}}%",
    },
    fonts: {
      bundled: "Bundled",
      installed: "Installed",
      showAll: "Show all fonts",
      showAllTip: "List every installed font family, not just fonts detected as monospace. Applies to both font pickers. Proportional fonts will misalign terminal output.",
    },
  },

  codeIntel: {
    automatic: "Automatic",
    onThisMachine: "on this machine",
    downloads: "termic downloads it",
    notInstalled: "not installed",
    install: "Install",
    installing: "Installing…",
    updateTo: "Update to {{version}}",
    downloading: "Downloading…",
    customCommand: "Custom command",
    customSet: "Used instead of the servers above. Clear it to go back to them.",
    machineOverride: "This machine: {{value}}. Set one here to override it.",
    customHint: "Runs it as typed, with no shell. Quotes group an argument that has spaces.",
    check: "Check for server updates",
    checking: "Checking…",
    checkNote: "Only when you ask. Nothing checks in the background.",
    noneInstallable: "No servers can be installed on this platform.",
    zubanToast: "zuban is installed. Python tasks pick it up when their server next starts.",
    updateToast: "{{label}} {{version}} is installed. Tasks pick it up when their server next starts.",
    configHeading: "Server Configuration",
    configDesc: "Learn how to configure your running servers or provide advanced JSON overrides if needed.",
    configFiles: "Configuration files:",
    ignoring: "Ignoring paths:",
    envVars: "Environment variables:",
    readDocs: "Read {{name}} documentation",
    advanced: "Advanced overrides",
    advancedHint: "Only use this for settings that exist purely over LSP and cannot be configured via files. Sent as <1>{{channel}}</1>.",
    appliedNext: "Applied the next time this server starts.",
    runningOne: "This server is running with the settings it started with.",
    runningMany: "{{count}} copies of this server are running with the settings they started with.",
    restart: "Restart to apply",
    restarting: "Restarting…",
  },

  shortcuts: {
    sub: "Click a shortcut to rebind it. Press Esc while recording to cancel.",
    resetAll: "Reset all",
    conflict: "Conflicts with another shortcut",
    reservedTab: "Tab belongs to the recently-used-tabs gesture.",
    needsMod: "Add {{cmd}} or {{alt}} to the combo.",
    pressKeys: "Press keys…",
    resetDefault: "Reset to default",
    doubleShiftAria: "When double-Shift opens Search everywhere",
    ctrlTabAria: "Whether Ctrl+Tab walks the recently used tabs",
  },

  profiles: {
    desc1: "A profile is a fully separate Termic: its own projects, tasks, settings and agents, in its own window. Two can be open at once and neither sees the other's work.",
    desc2: "Agent logins are the one thing they share: you sign in once, on this machine.",
    emptyTitle: "You have one setup, and no profiles yet.",
    empty1: "Creating your first profile also names the one you are using now, so you can tell the two windows apart.",
    empty2: "Your projects, tasks and settings stay exactly where they are.",
    createFirst: "Create a profile",
    newProfile: "New profile",
    disable: "Stop using profiles",
    disableNote: "\"Stop using profiles\" keeps every project, task and setting exactly where it is. It only stops giving this window a name.",
    thisWindow: "This window",
    focus: "Focus",
    closeFirst: "Close this window first",
    deleteProfile: "Delete profile",
  },

  prompts: {
    desc: "Reusable prompts for the Prompts menu in the top bar. When you fire one, you pick where it goes: an existing agent (queued if it is busy) or a new agent. Drag to reorder. Built-ins can be edited and reset. Open the palette with ⌥⌘P to search and fire them by keyboard.",
    new: "New prompt",
    empty: "No prompts. Add one, or restore the built-ins below.",
    dragTip: "Drag to reorder",
    titlePlaceholder: "Prompt title",
    builtIn: "Built-in",
    modified: "Modified",
    editTip: "Edit prompt",
    disableTip: "Disable (hide from menu)",
    duplicateTip: "Duplicate",
    resetTip: "Reset to built-in text",
    emptyPrompt: "Empty prompt. Click to edit.",
    restore: "Restore built-in prompts ({{count}})",
    dialogTitle: "Edit prompt",
    bodyPlaceholder: "Prompt text sent to the agent…",
    incomplete: "Title and prompt text are both required.",
    autosave: "Changes save automatically.",
    resetButton: "Reset to built-in",
    resetTitle: "Reset \"{{title}}\" to default?",
    resetMessage: "This discards your edits to this built-in prompt and restores the shipped text.",
    restoreTitle: "Restore deleted built-in prompts?",
    restoreMessage: "Brings back the built-in prompts you deleted. Your custom prompts and edits stay as they are.",
    restoreLabel: "Restore",
  },

  agents: {
    intro: "Customize the command and flags used to launch each agent. Useful when the CLI renames a flag (e.g., a future <1>--yolo</1> rename) or you want to point at a wrapper script. Built-in agents can be edited but not removed. Custom terminals appear under \"New terminal\" in the + tab menu and run their command line through your login shell (handy for devcontainers: <3>docker exec</3>, ssh boxes, REPLs).",
    resetBuiltins: "Reset built-ins",
    resetBuiltinsTip: "Reset the built-in agents to ship defaults (custom agents kept)",
    addAgent: "Add agent CLI",
    addTerminal: "Add terminal",
    addTerminalTip: "Add a custom terminal: a command line (docker exec, ssh, ...) offered under New terminal in the + tab menu",
    resetAllTitle: "Reset built-in agents to defaults?",
    resetAllMessage: "Resets the built-in agents (claude, codex, Antigravity, gemini) to their ship-default commands. Custom agents, per-agent env blocks and your credential sets are kept.",
    removeAgentTitle: "Remove agent?",
    removeTerminalTitle: "Remove terminal?",
    removeBody: "<1>{{name}}</1> will be removed. Tasks that reference it will fall back to spawning the literal command <3>{{command}}</3>.",
    pinnedOne: "It is the default CLI for <1>{{name}}</1>, which will switch to <3>{{fallback}}</3>.",
    pinnedMany: "It is the default CLI for {{count}} projects, which will switch to <1>{{fallback}}</1>.",
    terminalsGroup: "Terminals",
    newAgentName: "New agent {{n}}",
    newTerminalName: "New terminal {{n}}",
    allTerminals: "All terminals",
    copyOnSelect: {
      label: "Copy on select",
      hint: "iTerm-style: selecting text with the mouse in any terminal copies it to the clipboard automatically. Applies to every terminal (agents and scratch shells).",
    },
    card: {
      modifiedTip: "Modified from ship defaults",
      builtInBadge: "built-in",
      terminalBadge: "terminal",
      terminalBadgeTip: "Custom terminal: offered under New terminal in the + tab menu. Runs through your login shell; no agent features (resume, work-done, message queue).",
      extendsBadge: "extends: {{name}}",
      // {{count}} keys with singular/plural distinction in the pre-i18n UI use
      // i18next's sibling _one/_other forms; zh keeps one text in both.
      inheritsSomeTip_one: "Inherits from {{name}}. {{count}} field overridden; everything else follows {{name}} as it changes.",
      inheritsSomeTip_other: "Inherits from {{name}}. {{count}} fields overridden; everything else follows {{name}} as it changes.",
      inheritsAllTip: "Inherits everything from {{name}}, live. Editing a field here overrides just that one.",
      overridesReset_one: "{{count}} override · reset",
      overridesReset_other: "{{count}} overrides · reset",
      overridesResetTip_one: "Clear all {{count}} override and inherit everything from {{name}} again.",
      overridesResetTip_other: "Clear all {{count}} overrides and inherit everything from {{name}} again.",
      modifiedBadge: "modified",
      modifiedBadgeTip: "Some fields differ from this agent's ship defaults. Use Reset to revert.",
      installedBadge: "installed",
      notFoundBadge: "not found",
      foundTip: "Found: {{path}}",
      onPath: "on PATH",
      notFoundTip: "Not found on PATH. Hidden from the CLI pickers until it's installed (or point Command at an absolute path).",
      showTip: "Shown in the CLI pickers. Click to hide.",
      hideTip: "Hidden from the CLI pickers. Click to show.",
      resetConfirm: "Reset {{name}} to ship defaults?\n\nThis overwrites Command, Default args, YOLO args, Runtime YOLO command, and Resume args.",
      resetTip: "Reset this agent to ship defaults",
      cloneTip: "Clone this agent: the copy INHERITS every setting and follows this agent as it changes. Fill a field on the copy to override just that one.",
      removeTip: "Remove agent",
      inheritBanner: "This agent inherits everything from <1>{{name}}</1>. Every field left empty uses {{name}}'s current value, shown greyed below, and follows it as {{name}} changes. Fill a field in only to override that one.",
      inheritBannerCount_one: " You are overriding {{count}} field right now.",
      inheritBannerCount_other: " You are overriding {{count}} fields right now.",
      commandLabel: "Command",
      commandHintTerminal: "Run through your login shell (quoting, pipes, and rc-file PATH all work). The shell stays interactive after the command exits. Placeholders: {task_slug}, {task_name}, {task_path}, {branch}, {port}.",
      commandHintAgent: "Single executable to spawn (PATH lookup or absolute path). No shell parsing - quoted/piped strings won't work, and shell-style `VAR=val cmd` prefixes won't either; use the Environment box below for env vars.",
      defaultArgsLabel: "Default args",
      defaultArgsHintTerminal: "Appended to the command line above. Space-separated. Same placeholders, including {task_path} (differs between the main repo and each worktree).",
      defaultArgsHintAgent: "Always passed. Space-separated. Placeholders: {task_slug}, {task_name}, {task_id}, {task_path}, {branch}, {port}.",
      yoloLabel: "YOLO args",
      yoloHint: "Appended when YOLO mode (⚡) is on. Empty = no flag added.",
      runtimeYoloLabel: "Runtime YOLO command",
      runtimeYoloHint: "Slash-command sent to the live agent to switch it into YOLO. Empty = the YOLO toggle needs a respawn.",
      runtimeDefaultLabel: "Runtime default command",
      runtimeDefaultHint: "Slash-command sent to switch the live agent back to default (YOLO off). Empty = needs a respawn.",
      resumeLabel: "Resume last (worktrees)",
      resumeHint: "CWD-based resume. Used on every spawn after the first inside a worktree task (each worktree has its own dir, so the agent's most-recent CWD session IS this task's session). Not used in main-checkout tasks (the shared dir would lasso external sessions; the main checkout uses Session/Resume ID args instead).",
      sessionIdLabel: "Session ID args (main checkout)",
      sessionIdHint: "First spawn in a main-checkout task, mints a termic-owned uuid. Use {UUID}. Empty = no auto-resume in the main checkout for this agent.",
      resumeIdLabel: "Resume ID args (main checkout)",
      resumeIdHint: "Every spawn after the first in a main-checkout task. Resumes the termic-owned uuid (isolates us from external sessions in the same cwd). Use {UUID}.",
      resumePickerLabel: "Session picker args",
      resumePickerHint: "Opens the agent's own session picker when a stored session fails to resume, instead of starting a new session. The session you pick comes back through the agent hooks, so Termic resumes it next time. No {UUID}. Empty = start a new session, as before; a built-in agent gets its default back on the next start, like the other argument lists.",
      nameArgsLabel: "Name args",
      nameArgsHint: "Applied on every spawn. Pins a display name for the session (claude shows it in /resume and the prompt box). Placeholders supported: {WORKSPACE_SLUG}, {WORKSPACE_NAME}, {BRANCH}.",
      envLabel: "Environment",
      envHint: "One KEY=VALUE per line, merged on top of the inherited parent env. `~/` expands to your home dir. `#` lines are ignored. Kept across Reset.",
      dockerEnvLabel: "Environment (Docker)",
      dockerEnvHint: "A SEPARATE list, used instead of the one above when this agent runs in a container. Empty = the list above is used as-is. Worth filling in when a value names a path on your Mac, since that path does not exist inside the container.",
      sandboxPathsLabel: "Sandbox allowed paths",
      sandboxPathsHint: "One path per line. $HOME and ~ expand. Joined into every task sandbox that uses this agent; tasks cannot remove them. Reset to defaults restores the shipped list.",
      sandboxHostsLabel: "Sandbox allowed hosts",
      sandboxHostsHint: "One host per line; * is a wildcard (e.g. *.mycompany.com). Joined into every task sandbox that uses this agent. This is where 'Allow · per agent' in the activity popover saves hosts.",
      workDoneTitle: "Work-done detection",
      workDoneHint: "When off, the done badge, bell, and OS notification are never shown for this agent. Turn it off for a custom CLI whose signals cause false positives.",
      on: "On",
      off: "Off",
      signalHintBuiltin: "How termic reads this agent's state from its terminal title. One regex per line. The greyed patterns are what it uses today. Filling in one field replaces only that field, the other two keep the patterns shown. To switch a field off entirely, give it a pattern that never matches, such as (?!). When several match, attention wins over busy, and busy over done.",
      signalHintCustom: "How termic reads this agent's state from its terminal title. One regex per line. The greyed patterns are examples (this agent ships no title heuristics of its own). When several match, attention wins over busy, and busy over done.",
      doneLabel: "Done (title → done)",
      doneHint: "Marks the turn finished: blue badge, bell, notification.",
      busyLabel: "Busy (title → working)",
      busyHint: "Marks the agent as working (spinner), and holds off the idle heuristics while it runs.",
      attentionLabel: "Attention (title → needs you)",
      attentionHint: "The agent is blocked on you: bell + attention dot. Wins over the other two.",
      pendingLabel: "Still working (screen → not done yet)",
      pendingHint: "Matched against the BOTTOM of the screen, not the title. While one of these matches, the done badge is held back. For agents that background work and end their turn anyway, so the title says idle while the job runs.",
      matchLabel: "Match the patterns above against output too",
      matchHintOn: "The patterns are matched against the terminal title only. Turn this on for a CLI that prints its status to stdout and never sets a title, and every line of output gets tested as well. Costs a little on very chatty agents. Takes effect on the next terminal restart, not on open terminals.",
      matchHintOff: "Nothing to match yet. This scans output for the patterns above, so it needs at least one of them filled in. The built-in title heuristics don't apply here, they describe a title, not a line of output.",
      matchNone: "No patterns to match",
      matchBoth: "Title and output",
      matchTitle: "Title only",
      invalidRegex: "Ignored (invalid regex):",
      footerUsage: "Show plan usage in the footer",
      footerUsageHint: "How much of this account's rolling limits is spent (session and weekly), next to the account name.",
      footerUsageNone: "This agent has no usage source yet, so there is nothing to show either way.",
      footerContext: "Show context window in the footer",
      footerContextHint: "How full the current conversation is, from the tab of this agent you used last. Click the chip for the token counts.",
      footerContextNone: "This agent does not report its context window, so there is nothing to show either way.",
    },
    accounts: {
      firstLabel: "Name your current login",
      label: "Credentials",
      defaultBadge: "default",
      notSignedIn: "not signed in",
      defaultTip: "Used by new tasks",
      useTip: "Use this one for new tasks",
      removeAria: "Remove {{name}}",
      errSecond: "Name the second account too.",
      andAdd: "and add",
      placeholderPersonal: "Personal",
      placeholderWork: "Work",
      addLogin: "Add a second login",
      ordinaryLogin: "this agent's ordinary login",
      defaultNote: "New tasks use <1>{{name}}</1>. Click another name to make it the default, or switch one task from its footer.",
      sharedEnv: "Switching sets <1>{{var}}</1>, which other tools in the same environment also read.",
      secondAccount: "Second account",
      secondAccountTip: "Sign in to this agent with a second account and switch between them per task. Nothing is copied: each account keeps its own login, and the one you have now keeps working.",
    },
    hooks: {
      title: "Agent hooks",
      partialTip: "Some detected agents are not reporting their own state. Expand to see which.",
      summaryCount: "{{installed}} of {{total}} installed",
      summaryNone: "Let agents report their own state",
      autoLabel: "Install hooks for every agent",
      autoHint: "Includes agents you add later.",
      desc: "Termic installs a small script into the agent's own config so it reports when a turn starts, when it needs you, and when it is done. Without it Termic infers all three from the terminal, which is usually right: Claude paints its idle glyph while blocked on a permission prompt, and again while its subagents run, so a task can read as finished when it is not. Removal puts the config back byte for byte, and each row shows exactly what it writes.",
      blocked: "disableAllHooks is set in this config",
      installed: "installed",
      notInstalled: "not installed",
      install: "Install",
      showInstalls: "Show exactly what this installs",
      hideInstalls: "Hide what this installs",
      yoursMerges: "yours; termic merges into it",
      runsOn: "Runs on {{events}}",
    },
    signals: {
      show: "Show what this agent is emitting…",
      observed: "Observed titles",
      capture: "Capture a turn",
      captureAgain: "Capture again",
      waiting: "Send one prompt to a task running this agent. Recording stops on its own when the turn finishes.",
      recording: "Recording the turn…",
      empty: "Nothing seen yet. Start a task with this agent and its terminal titles show up here.",
      footnote: "Adding from here inserts the title as an exact match. A capture proposes patterns that cover a whole spinner instead.",
      emptyTitle: "(empty title)",
      unmatched: "unmatched",
      classBusy: "Busy",
      classDone: "Done",
      classAttention: "Needs you",
      copyTitle: "Copy this title",
      addAs: "Add as {{class}}",
      proposals: "Suggested from that turn",
      nothingUsable: "Nothing usable. The agent may not set a terminal title at all, in which case turn on output matching below and try again.",
      kindGlyph: "covers the spinner",
      kindText: "shared text",
      kindExact: "exact title",
      use: "Use",
      skipped_one: "Skipped {{count}} suggestion that would also have matched another state (e.g. <1>{{pattern}}</1> matches <3>{{conflict}}</3>).",
      skipped_other: "Skipped {{count}} suggestions that would also have matched another state (e.g. <1>{{pattern}}</1> matches <3>{{conflict}}</3>).",
    },
  },

  docker: {
    intro: "The same agents you configure in Settings → Agents & Terminals, run inside a container instead of under Seatbelt. One image serves all of them; pick Docker per task from its sandbox dialog.",
    fsLabel: "Filesystem: ",
    fsBody: "only what termic mounts: the worktree, its git metadata, and your git name and email so commits you make in there are yours. The rest of your Mac is invisible.",
    netLabel: "Network: unrestricted for now",
    netBody: ", unlike Seatbelt's host allowlist. An allow-list for Docker mode is planned.",
    loginsLabel: "Logins: ",
    loginsBody: "kept per agent, so you log in once rather than once per task. Never your real <1>~/.claude</1>.",
    firstRun: {
      title: "Docker sandbox",
      hint: "Runs agents inside a container instead of the macOS seatbelt. Needs a one-time image build, a few minutes, and Docker running. Existing tasks are unaffected: you pick Docker per task afterwards.",
      building: "Building the image…",
      enableAndBuild: "Enable Docker sandboxing and build image",
      notInstalled: "Docker isn't installed. Install Docker Desktop, then come back.",
      notRunning: "Docker is installed but not running. Start Docker Desktop, then come back.",
    },
    enable: {
      label: "Enable Docker sandbox",
      hint: "While off, no Docker UI appears anywhere and Docker is never invoked. The built image is kept, so turning it back on costs nothing.",
    },
    imageHeading: "Image",
    dockerfileHeading: "Dockerfile",
    hideDockerfile: "Hide the Dockerfile",
    editDockerfile: "Edit the Dockerfile",
    unsaved: "Unsaved edits",
    dfDefault: "Default",
    dfCustomized: "Customized",
    imageLabel: "Image",
    dockerLabel: "Docker",
    cliWarn: "This Dockerfile does not install the GitHub / GitLab CLIs. Agents in a container will not have <1>gh</1> or <3>glab</3>. Reset to default to pick them up, or add them yourself.",
    editorHint: "One generic image for all agents. Edit the commented regions to add MCP servers, CLI tools, or baked skills. Personal logins (agent auth, MCP OAuth) are NOT set up here, just run the agent and log in once inside Docker; those persist via your mounted config directory.",
    resetDefault: "Reset to default",
    buildImage: "Build image",
    rebuildImage: "Rebuild image",
    updateAgents: "Update agents",
    buildTipNoCache: "Rebuilds from scratch, which is what picks up newer agent versions: the image installs them unpinned, so a cached rebuild would change nothing.",
    buildTipCached: "Builds the image from the Dockerfile, reusing any layers that have not changed.",
    building: "Building…",
    startToBuild: "Start Docker to build.",
    staleWarn: "Dockerfile changed since this image was built.",
    showLog: "Show log",
    hideLog: "Hide log",
    buildOk: "✓ Build finished.",
    buildFail: "✗ Build failed.",
    rebuild: {
      title: "Rebuild frequency",
      hint: "How often the image is considered out of date. When a Docker-mode agent launches and the image has not been rebuilt within this window, termic asks first, or just does it in the background if you tick the box below. Rebuilding is the only thing that updates an agent CLI: binaries live in the image, and a container is destroyed when its terminal closes, so an agent that updates itself mid-session loses that update on the next launch.",
      auto: "Rebuild automatically.",
      autoBody: "On by default. Keeps the image current on this schedule without asking, in the background, so agents launch straight away and pick up the new image next time. Turn it off to be asked before each rebuild instead.",
    },
    preview: {
      title: "Command preview",
      sub: "The exact <1>docker run</1> a task launch builds, agent command included.",
      agentLabel: "Agent",
      firstAgent: "First enabled agent",
      footnote: "Your task's worktree is mounted in place of the placeholder path above. Everything else, the mounts, the environment and the hardening flags, is what a real launch uses.",
    },
    configHeading: "Configuration",
    persisted: {
      title: "Persisted directories & environment",
      sub: "Directories that survive a container restart, shared or per agent, and the environment each one runs with.",
      fact1: "Kept across container restarts. Each entry is a path <1>inside the container</1>: a bare name means the agent's home there, so <3>.claude</3> is <5>/root/.claude</5>. Full paths like <7>/data/models</7> work too.",
      fact2: "Backed by a folder termic owns, never your real <1>~/.claude</1>. Per-agent by default, so cloning an agent gets it a separate one and a work login stays apart from a personal one; the <3>All agents</3> row is the exception, one folder every agent shares.",
      fact3: "<1>/root</1> is only where HOME points. The container runs as your own user, with no <3>sudo</3> and no way to elevate.",
      allAgents: "All agents",
      sharedEmpty: "Nothing shared. Agents in a container will have no <1>gh</1> / <3>glab</3> login.",
      sharedWhy: "Mounted into every container, for every agent, one folder each. A GitHub token is yours rather than any one agent's, so <1>gh auth login</1> once inside any Docker task covers them all. Your own <3>~/.config/gh</3> is never mounted.",
      lockTip: "Built-in - confirmed to hold real state, can't be removed",
      removeDir: "Remove {{dir}}",
      addDir: "+ add",
      dirPlaceholder: ".mytool or /data/cache",
      persist: "Persist config in Docker mode",
      persistWarn: "(add a dir above, nothing is mounted yet)",
      persistUnsupported: "Not supported in Docker mode: its config and its binary share a location, so mounting anything here would risk hiding the binary the image installed.",
      envLabel: "Environment (Docker)",
      envCount: "{{count}} set",
      envPlaceholder: "KEY=VALUE, one per line\nEmpty = this agent's normal environment",
    },
    mounts: {
      title: "Default extra mounts",
      hint: "Bind-mount these host directories into every NEW Docker-sandboxed task by default, one per line as <1>host_path:container_path</1> (same field and format as a task's own \"Extra mounts\"). The user can edit, remove, or add to them per task afterward - editing this list only affects tasks created from now on.",
      label: "Extra mounts",
    },
    availability: {
      checking: "Checking…",
      notFound: "`docker` not found on PATH. Install Docker Desktop, OrbStack, or colima.",
      daemonDown: "Docker is installed but the daemon is not running. Start it to build / run.",
      ready: "Ready",
    },
    builtPrefix: "Built · ",
    notBuilt: "Not built yet. Build it to use Docker mode in a task.",
  },

  repo: {
    notFound: "Project not found.",
    yamlError: "Couldn't read .termic.yaml: {{error}}",
    removeNoTasks: "No tasks to remove.",
    removeArchives_one: "Archives {{count}} task.",
    removeArchives_other: "Archives {{count}} tasks.",
    removeWorktrees_one: "{{count}} git worktree removed from disk (rm -rf).",
    removeWorktrees_other: "{{count}} git worktrees removed from disk (rm -rf).",
    removeMainEntries_one: "{{count}} main-checkout entry unregistered.",
    removeMainEntries_other: "{{count}} main-checkout entries unregistered.",
    removeUntouched: "Your repo at {{path}} is NOT touched. Cannot be undone.",
    removeTitle: "Remove project \"{{name}}\"?",
    removeConfirm: "Remove project",
    removeBusy_one: "Removing \"{{name}}\" and {{count}} task…",
    removeBusy_other: "Removing \"{{name}}\" and {{count}} tasks…",
    tabScripts: "Scripts & run",
    tabScriptsMulti: "Members & scripts",
    tabGit: "Git",
    tabMore: "More",
    targetPersonal: "Personal",
    targetPersonalHint: "overrides when set",
    targetPersonalSandboxHint: "local only · merged on top",
    targetYaml: ".termic.yaml",
    targetYamlHint: "committed to git repo",
    previewLabel: "Preview URL",
    previewHint: "URL the terminal panel's Open button opens. Supports <1>$TERMIC_WORKSPACE_NAME</1>, <3>$TERMIC_PORT</3>, etc. Extra named ports work here too (e.g. <5>$API_PORT</5>). Blank = auto-detect from output logs.",
    browserLabel: "Open links in",
    browserHint: "Which browser this project uses for the Preview URL above, the Open button on the run toolbar, and links you {{mod}}-click in a terminal. Useful when one project needs a different browser (or browser profile) from the rest.",
    setupLabel: "Setup script",
    setupHint: "Runs when a new task is created.",
    runLabel: "Run script",
    runHint: "Runs when you click the Run button. Use <1>$TERMIC_PORT</1> so each task gets its own port. Extra named ports are available under their own names.",
    archiveLabel: "Archive script",
    archiveHint: "Runs before a task is archived. Termic already removes the worktree dir + its contents, so this is for stopping external services your run script started.",
    portsLabel: "Extra named ports",
    portsHint: "One env var name per line (e.g. <1>API_PORT</1>). Each task gets its own unique port for every name, exposed to scripts, agents and the preview URL under exactly that name, alongside <3>$TERMIC_PORT</3>. Existing tasks pick up newly added names on their next run or terminal; ports already assigned never change.",
    portsWarning: "Ignored (invalid or reserved): {{names}}",
    runCmdLabel: "Run commands",
    runCmdHint: "Extra commands shown in the Run dropdown, each opening its own run tab. Separate from the single Run script above. Manage personal + shared commands, and test them, in one place. You can also add one by right-clicking a file in the tree.",
    runCmdButton: "Run configuration…",
    filesLabel: "Files to copy",
    yoloDefault: {
      label: "New tasks start in YOLO",
      hint: "Whether new tasks of this project skip the agent's own permission prompts. The New Task dialog starts here and lets you change it per task. Saved on this machine only, not in <1>.termic.yaml</1>.",
      inherit: "App default ({{value}})",
      on: "On",
      off: "Off",
    },
    filesHintMulti: "Copied from this project's own repo root into the root of each new task. One per line, glob patterns OK (e.g. <1>.env*</1> at the root, <3>**/.env*</3> at any depth). Members get their own list, in Members & scripts above.",
    filesHintSingle: "Copied from the repo root into each new task. One per line, glob patterns OK (e.g. <1>.env*</1> at the root, <3>**/.env*</3> at any depth).",
    hiddenLabel: "Hidden files",
    hiddenHint: "Patterns hidden from the \"All files\" tree for this repo. Saved to <1>.termic.yaml</1> (committed, team-shared) and merged with your personal list (Settings → General).",
    intelIntro: "A language server runs per CHECKOUT, from your own toolchain, and holds its index (hundreds of megabytes to several gigabytes) until it stops.",
    autoHeading: "Auto start",
    autoHint: "Arm new tasks for this project without asking in each one. Which checkouts that covers decides how many servers can end up running.",
    autoOff: "Off",
    autoOffHint: "Each task asks. Nothing runs until you say so.",
    autoMain: "Main checkout only",
    autoMainHint: "One server per language, ever. Every task on the main checkout shares it, so the tenth costs what the first did.",
    autoAll: "Main checkout and worktrees",
    autoAllHint: "One server per language PER WORKTREE, and it multiplies: four languages across ten worktrees would be forty. rust-analyzer alone holds about 3 GB, so termic starts at most 6 this way and leaves the rest to the button on the editor, where each one's cost is shown.",
    languagesHeading: "Languages",
    languagesHint: "Which of them start on their own, ticked from what this repo turned out to be written in. Each server is its own process with its own memory, so a repo that is several languages (a Django project has Python and the JavaScript in its templates) is several servers. Unticking one only stops it starting by itself: the button on the editor still offers it, with its cost.",
    detecting: "Looking at the repo…",
    serversHeading: "Language servers for this project",
    serversHint: "Leave everything on Automatic to follow your machine's settings.",
    sandboxHeading: "Sandbox",
    sandboxIntro: "When a task is sandboxed, the agent runs under macOS seatbelt: the filesystem is allow-listed (task + agent state + caches + dirs you list); HTTPS goes through an in-process per-task proxy filtered against the host allowlist. Secrets (<1>~/.ssh</1>, <3>~/.aws</3>, <5>~/.gnupg</5>, <7>~/.netrc</7>, <9>~/.kube</9>, …) and personal data (<11>~/Documents</11>, <13>~/Desktop</13>, <15>~/Downloads</15>, browser data, mail) are denied by default.",
    defaultLabel: "New tasks default to",
    defaultHint: "Applied to every task created for this project, including the quick + menu in the sidebar. The New Task dialog starts here and lets you change it per task.",
    dockerUnavailable: "Turn Docker sandboxing on and build the image in Settings → Docker Sandbox first.",
    mountsLabel: "Default extra mounts",
    mountsHint: "Bind-mounted into every new Docker task of this project, one per line as <1>host_path:container_path</1>. Leave empty to use the global list from Settings → Docker Sandbox. Editing this only affects tasks created from now on.",
    allowedPathsLabel: "Allowed paths",
    allowedPathsHint: "Dirs the agent can read AND write. One per line. ~, $HOME, and $WORKSPACE expand at spawn time.",
    allowedHostsLabel: "Allowed hosts",
    allowedHostsHint: "One per line. Use * as a wildcard. Per-CLI vendor + GitHub + npm/pypi/crates.io are always allowed; these are extras.",
    branchLabel: "Branch new tasks from",
    branchHint: "Each task is an isolated copy of your codebase, branched off here. The project + menu writes this too.",
    remoteLabel: "Remote",
    remoteHint: "Git remote name (used when resolving the base branch).",
    mergeLabel: "When a pull request merges",
    mergeHint: "Termic watches the PR/MR of each task you look at. On merge it can offer to archive the task (toast with an Archive button), archive it automatically, or do nothing.",
    mergeAsk: "Ask (toast)",
    mergeAuto: "Archive automatically",
    mergeOff: "Do nothing",
    watchLabel: "Watch PR comments",
    watchHint: "When a launched task of this project has a pull request, new comments are automatically queued into its main agent to address. Equivalent to switching on the bell on every PR card. Off = opt in per task.",
    watchPerTask: "Per task (bell)",
    watchAlways: "Always",
    actLabel: "Act on comments from",
    actHint: "A PR/MR comment gets fed to the agent to address, with real shell access. Anyone who can see a pull request can usually comment on it regardless of repo permissions, so by default only commenters with verified standing (owner, member, collaborator) are acted on.",
    actCollaborators: "Collaborators only",
    actEveryone: "Everyone",
    spotlightHeading: "Spotlight",
    spotlightUnsupported: "Spotlight is not supported for multi-repo projects.",
    spotlightEnable: "Enable spotlight for this project",
    spotlightHint: "When enabled, you can spotlight a task from its settings menu. Spotlight syncs that task's changes to your main checkout automatically so you can run and test from there. Committed changes appear as a checkpoint commit on main; uncommitted edits sync as working-tree changes; untracked files are copied (.gitignore respected). Main must be clean to start. Stopping spotlight removes the checkpoint commit and restores main. While spotlight is active, the run script executes at the repo root.",
    spotlightActive: "<1>{{name}}</1> is spotlighted right now",
    spotlightNone: "No task is spotlighted right now.",
    defaultCliLabel: "Default CLI",
    defaultCliHint: "Which agent to spawn for new tasks in this repo. Pick Terminal for a plain login shell (no agent).",
    cliMissing: "{{cli}} (not in your agents list)",
    cliNotSet: "Not set",
    cliTerminal: "Terminal",
    rootLabel: "Root path",
    rootHint: "The git repo on disk. Do not move or delete this directory; remove the project in Termic instead.",
    tasksPathLabel: "Tasks path",
    tasksPathHint: "Where this repo's new worktrees live. Leave it empty to follow the default tasks path. A value here overrides that for this repo only: a full path (starting with / or ~) becomes the worktree root as-is, a relative one resolves inside the repo. Don't move or delete subdirectories; archive tasks in Termic instead.",
    tasksPathConflict: "This lands inside the repo itself, so new tasks would be created on top of your working tree. Changes here are not being saved until you pick a directory outside the repo, or a subdirectory of it.",
    removeLabel: "Remove project",
    removeHint: "Drops the project from the sidebar + archives every task under it. The actual repo at <1>{{path}}</1> stays on disk.",
    membersTitle: "Members & scripts",
    membersDesc: "Repos to mount inside every task under this multi-repo project, the <1>Setup / Run / Archive</1> commands to use for each, and the <3>Files</3> each one needs copied from its repo root into its worktree (gitignored things a build needs: <5>.env</5>, keystores, service-account keys). These live on the multi-repo project, independent of the member project's own scripts; leave Files empty and that repo's own committed <7>.termic.yaml</7> list applies instead. Edits apply to <9>future</9> tasks; existing ones freeze at creation.",
    envVars: "Env vars:",
    membersEmpty: "No members yet. Add repos below: pick from your existing projects or add any folder from disk.",
    folderBadge: "folder",
    removeMemberTip: "Remove from this multi-repo project",
    setupShort: "Setup",
    runShort: "Run",
    archiveShort: "Archive",
    filesShort: "Files",
    filesTip: "Globs copied from this repo's root into its worktree, one per line. Empty falls back to the repo's own .termic.yaml list.",
    saveMembers: "Save members & scripts ({{count}})",
    membersSaved_one: "Updated {{count}} member on \"{{name}}\"",
    membersSaved_other: "Updated {{count}} members on \"{{name}}\"",
    addMember: "+ Add member",
    available: "Available repositories",
    pickerHint: "Pick one of your existing projects to copy in, or use “Add repo from disk” for any folder. Members are self-contained: each carries its own scripts, and nothing is registered as a standalone project.",
    addBadge: "Add",
    addFromDisk: "Add repo from disk",
    adding: "Adding…",
    addDiskNote: "Adds the folder as a member of this project only (no standalone project). A plain folder mounts as the main checkout only (no worktree).",
    tokenTip: "Click to select",
  },

  browserCommand: {
    presetAria: "Browser preset",
    commandAria: "Browser command",
    inheritWith: "Follow the app-wide setting ({{command}})",
    inheritDefault: "Follow the app-wide setting (system default)",
    custom: "Custom command…",
    placeholder: "Leave empty for your system default browser",
    urlHint: "The URL is added to the end of the command. Use <1>{url}</1> to put it somewhere else.",
  },

  exclude: {
    presetPython: "Python",
    presetNode: "Node / JS",
    presetRust: "Rust",
    presetBuild: "Build output",
    presetIde: "IDE / editor",
    presetMacos: "macOS",
    hint: "One glob per line. Matches a file/folder name at any depth (<1>node_modules</1>, <3>*.pyc</3>) or a path (<5>docs/build</5>). <7>.git</7> is always hidden.",
  },

  runCommands: {
    labelPlaceholder: "Label",
    add: "Add command",
  },
};
